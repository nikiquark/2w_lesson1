
// Должна вернуть строку "Hello world!"
function hello(){

}

// Сумма чисел а и b
function sum(a, b){

}

/*
    Допишите функцию, которая вычисляет случайное значение от min до max
    Math.random() - генерирует случайное число [0, 1)
    Math.floor(x) - округляет x вниз
*/ 
function myRand(min, max){

}


/*
    Допишите функцию, которая вычисляет факториал,
    т.е. произведение всех чисел от 1 до n
    1 * 2 * 3 * ... * n
    factorial(0) = 1;
*/ 
function factorial(n){

}

// Функция должна вернуть максимальный элемент из массива array
function maxElem(array){
    
}

module.exports = {
    hello,
    sum,
    myRand,
    factorial,
    maxElem
}